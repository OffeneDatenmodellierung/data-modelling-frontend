name = "data-modelling-frontend"
compatibility_date = "2024-01-01"
pages_build_output_dir = "frontend/dist"

[build]
command = "cd frontend && bash cloudflare-build.sh"

[env.production]
vars = { VITE_OFFLINE_MODE = "true", VITE_BASE_PATH = "/" }

[env.preview]
vars = { VITE_OFFLINE_MODE = "true", VITE_BASE_PATH = "/" }
